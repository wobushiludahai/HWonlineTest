
/**
 * @description: 无线OSS-高精度整数加法
 * @param {type} 
 * @return: 
 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

char str1[10000] = {0};
char str2[10000] = {0};
char str3[10000] = {0};
int strlen1, strlen2;

int cmpare(char *str1, char *str2)
{
    
}

int main(void)
{
    while(scanf("%s", str1) != EOF)
    {
        scanf("%s", str2);
        strlen1 = strlen(str1);
        strlen2 = strlen(str2);

    }

    return 0;
}
